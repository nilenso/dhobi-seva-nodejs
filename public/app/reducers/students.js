const INITIAL_STATE = {
  courseId: '',
  students: []
}


const students = (state = INITIAL_STATE, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default students;
